#include <stdio.h>

 /**
 * main - lower case
 * putchar cuz no printf, puts
 * Return: one day i will
 */
	int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
{
		putchar(ch);
}
		putchar('\n');
	return (0);
}

